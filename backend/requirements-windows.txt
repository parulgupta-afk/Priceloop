# Prefer Python 3.11 or 3.12 on Windows.
# If lxml fails: pip install lxml --only-binary=:all:
# If orjson fails: pip install orjson --only-binary=:all:  OR skip it (optional)
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3
pgvector==0.3.4
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi>=23.1.0
redis==5.0.8
celery==5.4.0
httpx==0.27.2
beautifulsoup4==4.12.3
tenacity==9.0.0
python-dotenv==1.0.1
pytest==8.3.3
pytest-asyncio==0.24.0
# Install these with binary wheels if build tools are missing:
# pip install lxml --only-binary=:all:
# pip install orjson --only-binary=:all:
